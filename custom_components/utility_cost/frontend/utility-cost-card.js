class UtilityCostCard extends HTMLElement {
  setConfig(config){this.config=config||{};this.period=this.period||"today";this.openDevice=null;this.editRates=false;if(!this.shadowRoot)this.attachShadow({mode:"open"});}
  set hass(h){this._h=h;this.render();} getCardSize(){return 12;} static getStubConfig(){return {};}
  findRole(period,role){return Object.values(this._h?.states||{}).find(x=>x.attributes?.period===period&&x.attributes?.utility_cost_role===role);}
  money(v){const n=Number(v?.state??v);return Number.isFinite(n)?new Intl.NumberFormat('en-AU',{style:'currency',currency:'AUD'}).format(n):'—';}
  kwh(v){const n=Number(v);return Number.isFinite(n)?`${n.toFixed(n<10?2:1)} kWh`:'—';}
  label(p){return ({today:'Today',week:'Week',month:'Month',bill:'Bill period',year:'Year'})[p]||p;}
  tariffSensor(){return Object.values(this._h?.states||{}).find(x=>x.attributes?.friendly_name?.includes('Active Tariff')&&x.attributes?.plan);}
  deviceDetail(d){const tk=d.attributes.tariff_kwh||{},tc=d.attributes.tariff_cost||{},legacy=Number(d.attributes.legacy_unallocated_kwh||0),legacyCost=Number(d.attributes.legacy_unallocated_cost||0);return `<div class="detail">
    <div class="sub">Time-of-use</div><div class="drow"><span>Peak</span><span>${this.kwh(tk.peak)}</span><b>${this.money(tc.peak)}</b></div><div class="drow"><span>Shoulder</span><span>${this.kwh(tk.shoulder)}</span><b>${this.money(tc.shoulder)}</b></div><div class="drow"><span>Off-peak</span><span>${this.kwh(tk.offpeak)}</span><b>${this.money(tc.offpeak)}</b></div>${legacy>0.0005?`<div class="drow legacy"><span>Before TOU tracking</span><span>${this.kwh(legacy)}</span><b>${this.money(legacyCost)}</b></div>`:''}<div class="drow total"><span>Tariff value</span><span>${this.kwh(d.attributes.energy_kwh)}</span><b>${this.money(d)}</b></div>
    <div class="sub">Estimated bill impact</div><div class="drow"><span>Grid supplied</span><span>${this.kwh(d.attributes.estimated_grid_kwh)}</span><b>${this.money(d.attributes.estimated_grid_cost)}</b></div><div class="drow"><span>Solar supplied</span><span>${this.kwh(d.attributes.estimated_solar_kwh)}</span><b>${Number(d.attributes.solar_supplied_percent||0).toFixed(1)}%</b></div><div class="drow"><span>Lost solar FIT</span><span></span><b>${this.money(d.attributes.solar_opportunity_cost)}</b></div><div class="drow total"><span>Estimated bill impact</span><span></span><b>${this.money(d.attributes.estimated_bill_impact)}</b></div><div class="drow"><span>Estimated solar saving</span><span></span><b>${this.money(d.attributes.solar_saving)}</b></div>
    <div class="hint">Grid/solar attribution uses the whole-property net Fronius meter position at each interval, so it is phase-balanced and is an accounting estimate rather than circuit-level source tracing.</div></div>`;}
  render(){if(!this._h)return;const p=this.period,net=this.findRole(p,'net'),grid=this.findRole(p,'grid'),supply=this.findRole(p,'supply'),fit=this.findRole(p,'fit'),a=net?.attributes||{};const devices=Object.values(this._h.states).filter(x=>x.attributes?.source_entity&&x.attributes?.period===p).sort((x,y)=>Number(y.state)-Number(x.state));
    const deviceHtml=devices.length?devices.map(d=>`<div class="device"><button class="deviceHead" data-id="${d.entity_id}"><span>${d.attributes.display_name||d.attributes.source_entity}</span><span class="energy">${this.kwh(d.attributes.energy_kwh)}</span><b>${this.money(d.attributes.estimated_bill_impact)} bill</b><ha-icon icon="mdi:chevron-${this.openDevice===d.entity_id?'up':'down'}"></ha-icon></button>${this.openDevice===d.entity_id?this.deviceDetail(d):''}</div>`).join(''):'<div class="muted">No tracked devices configured.</div>';
    const rateBox=this.editRates?`<div class="rates"><div class="sub">Edit current rates</div>${[['peak_rate','Peak $/kWh'],['shoulder_rate','Shoulder $/kWh'],['offpeak_rate','Off-peak $/kWh'],['supply_daily','Supply $/day'],['fit_tier1','FIT tier 1 $/kWh'],['fit_tier2','FIT tier 2 $/kWh'],['fit_daily_limit','FIT tier 1 limit kWh']].map(([k,l])=>`<label>${l}<input type="number" step="0.000001" data-rate="${k}" value="${a.rates?.[k]??''}"></label>`).join('')}<button class="save">Save rates</button><span class="saveMsg"></span><div class="hint">Saved rates apply to new accumulation. Existing historical costs are not repriced.</div></div>`:'';
    this.shadowRoot.innerHTML=`<style>:host{display:block}.c{background:var(--ha-card-background,var(--card-background-color));border-radius:var(--ha-card-border-radius,12px);padding:20px;box-shadow:var(--ha-card-box-shadow)}.top{display:flex;justify-content:space-between;align-items:center}.actions{display:flex;gap:8px}.iconBtn,.save{border:0;border-radius:18px;padding:8px 12px;background:var(--secondary-background-color);color:var(--primary-text-color);cursor:pointer}.tabs{display:flex;gap:6px;margin:16px 0;flex-wrap:wrap}.tab{border:0;padding:8px 11px;border-radius:18px;cursor:pointer;background:var(--secondary-background-color);color:var(--primary-text-color)}.tab.on{background:var(--primary-color);color:var(--text-primary-color)}.big{font-size:34px;font-weight:650}.muted,.hint{color:var(--secondary-text-color)}.row,.drow{display:grid;grid-template-columns:1fr auto auto;gap:16px;padding:9px 0;border-bottom:1px solid var(--divider-color);align-items:center}.device{border-bottom:1px solid var(--divider-color)}.deviceHead{width:100%;display:grid;grid-template-columns:1fr auto auto 24px;gap:14px;align-items:center;padding:12px 0;border:0;background:none;color:var(--primary-text-color);text-align:left;font:inherit;cursor:pointer}.energy{color:var(--secondary-text-color)}.detail,.rates{padding:12px 14px;background:var(--secondary-background-color);border-radius:8px;margin-bottom:10px}.sub{font-weight:700;margin:8px 0}.drow{font-size:14px}.drow.total{font-weight:700;border-top:1px solid var(--divider-color)}.legacy{font-style:italic}.hint{font-size:12px;padding-top:9px}.rates{display:grid;grid-template-columns:repeat(2,minmax(160px,1fr));gap:10px}.rates .sub,.rates .hint{grid-column:1/-1}.rates label{display:flex;flex-direction:column;gap:4px;font-size:12px}.rates input{padding:8px;border:1px solid var(--divider-color);border-radius:6px;background:var(--card-background-color);color:var(--primary-text-color)}h3{margin:20px 0 6px}@media(max-width:600px){.deviceHead{grid-template-columns:1fr auto 22px}.deviceHead .energy{grid-row:2;grid-column:1/3;text-align:left}.row,.drow{grid-template-columns:1fr auto}.rates{grid-template-columns:1fr}}</style><ha-card><div class="c"><div class="top"><div><b>Utility Cost</b><div class="muted">${a.plan||''}</div></div><div class="actions"><button class="iconBtn edit">⚙ Rates</button></div></div>${rateBox}<div class="tabs">${['today','week','month','bill','year'].map(x=>`<button class="tab ${x===p?'on':''}" data-p="${x}">${this.label(x)}</button>`).join('')}</div><div class="muted">Estimated household bill position</div><div class="big">${this.money(net)}</div><h3>Actual bill estimate</h3><div class="row"><span>Grid import</span><span></span><b>${this.money(grid)}</b></div><div class="row"><span>Supply charge</span><span></span><b>${this.money(supply)}</b></div><div class="row"><span>Solar FIT credit</span><span></span><b>−${this.money(fit)}</b></div><h3>Tracked devices</h3>${deviceHtml}<div class="hint">The device headline shows estimated bill impact. Expand a device for tariff value, TOU use, grid/solar attribution, lost FIT and estimated solar saving.</div></div></ha-card>`;
    this.shadowRoot.querySelector('.edit').onclick=()=>{this.editRates=!this.editRates;this.render();};this.shadowRoot.querySelectorAll('.tab').forEach(e=>e.onclick=()=>{this.period=e.dataset.p;this.openDevice=null;this.render();});this.shadowRoot.querySelectorAll('.deviceHead').forEach(e=>e.onclick=()=>{this.openDevice=this.openDevice===e.dataset.id?null:e.dataset.id;this.render();});
    const save=this.shadowRoot.querySelector('.save');if(save)save.onclick=async()=>{const data={};this.shadowRoot.querySelectorAll('[data-rate]').forEach(i=>{if(i.value!=='')data[i.dataset.rate]=Number(i.value);});try{await this._h.callService('utility_cost','update_rates',data);this.config={...this.config,...data};this.shadowRoot.querySelector('.saveMsg').textContent='Saved';}catch(e){this.shadowRoot.querySelector('.saveMsg').textContent='Save failed';}};
  }
}
if(!customElements.get('utility-cost-card'))customElements.define('utility-cost-card',UtilityCostCard);window.customCards=window.customCards||[];if(!window.customCards.some(c=>c.type==='utility-cost-card'))window.customCards.push({type:'utility-cost-card',name:'Utility Cost',description:'Electricity bill, TOU and estimated per-device bill impact'});
